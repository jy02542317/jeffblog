using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;
using jeffBlog.Models;

namespace jeffBlog.Services
{
    public class Login
    {
        //private readonly MyBlogContext _context;

        //public Login(MyBlogContext context)
        //{
        //    _context = context;
        //}
        //public void Add(User user)
        //{
        //    using (var myBlogContext = new MyBlogContext())
        //    {
        //        myBlogContext.User.Add(user);
        //        myBlogContext.SaveChanges();
        //    }


        //}

        //public bool LoginSystem(string username, string password)
        //{

        //    using (var context = new MyBlogContext())
        //    {
        //        var User=context.User.SingleOrDefault(b => b.UserName == username && b.Password==password);
              
        //        if (User != null && User.IsValid == true)
        //        {
        //            return true;
        //        }
        //    }

        //    return false;
        //}
    }
}
